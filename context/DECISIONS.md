# Decisions — US Trends 2027 Microsite

Append-only log of notable scope, product, and design decisions. Newest at the bottom. Keep rows
short; link out to context/research/design files for detail. Don't rewrite past rows — supersede
them with a new row and mark the old one `Superseded`.

| Date | Decision | Why | Status |
|---|---|---|---|
| 2026-07-31 | Project scaffolded with `prez-proj` | Establish a repeatable structure | Active |
| 2026-07-31 | Flip cards chosen for brand/designer implications | Progressive reveal preferred over a list; keeps the section interactive in both live and async modes | Active |
| 2026-07-31 | Timeline tooltips (not inline text) for trend evolution | Keeps the timeline scannable; depth available on interaction without cluttering the layout | Active |
| 2026-07-31 | Accenture Song visual system (with Google Fonts substitutes for licensed typefaces) | Project is Accenture-internal; substitutes (Cormorant Garamond / DM Sans) match the visual roles of GT Sectra Fine / Graphik | Active |
| 2026-07-31 | Timeline and share dates are context-only — not shown on the site | Internal logistics; not relevant to the audience consuming the content | Active |
| 2026-08-05 | South has 3 regional trends (not 2); total is 11 not 10 | Confirmed by source PDF "Trends 2027 Pitches Combined" — South submitted Demise of the Monoculture, Neu-Urbanism and Agrario-Curiosity, and Technocratic Backlash and the Data Center War | Active |
| 2026-08-05 | Regional trend PDFs extracted to public/regional-trends/ as individual files | Gallery overlay on Methodology page displays each slide in an iframe on click | Active |
| 2026-08-13 | Scrollytelling Trend 1 built as a parallel route `/trend-1-scroll`; existing `/trend-1` and `/trend-2` left untouched | Requested as a copy so the current pages stay available for comparison and fallback | Active |
| 2026-08-13 | Alex Brush script typeface added for the Trend 1 scroll hero and watermark only | Figma frame 142:8559 specifies a script display face; user approved any similar script typeface, overriding the serif/sans pair in design.md for this one role | Active |
| 2026-08-13 | Word-drop accent palette introduced (`#05f2db`, `#60a5fa`, `#34d399`, `#ff50a0`, `#f87171`) | Taken from the Figma redesign; supersedes "purple is the only accent" from design.md for these annotation words. All five verified ≥5:1 on `#202020` | Active |
| 2026-08-13 | Purple gradient used as a large background fill on "What to expect in 2027" and "What this means" | Specified by the Figma redesign; supersedes design.md's "purple never a large background area" for these two sections | Active |
| 2026-08-13 | Three-node hover Timeline replaced on the scroll page by a self-signing SVG chart (Microtrend velocity vs Authorship signals, 2023–2027) | Figma annotation on node 110:29; y-axis is directional only, labelled "Not a measured index" | Active |
| 2026-08-13 | Signal images ported from Figma as provisional placeholders in `public/trend-1-scroll/` | Figma MCP asset URLs expire after 7 days; captions, source URLs and final files pending from the image spreadsheet | Active |
| 2026-08-13 | Section background changes cross-fade through full-width blend bands rather than cutting | User feedback: colour changes should happen gradually on scroll | Active |
| 2026-08-13 | Timeline curves generated as Catmull-Rom splines through the marker points; marker centres corrected to Figma dot centre (+4px, not the box corner) | Hand-drawn Béziers did not pass through the nodes, so PEAK/PATTERN/CROSSOVER floated off the lines. Now exact by construction, verified numerically | Active |
| 2026-08-13 | Chart line ends on an upward flick, not the "loop & tail" signature flourish | A self-intersecting loop reads as a rendering fault inside a plot area | Superseded |
| 2026-08-13 | Chart line ends in a cursive signature loop, generated parametrically and rotated onto the incoming stroke angle | Restores the "loop & tail" flourish in the Figma annotation (node 110:29). User reviewed three rendered alternatives and chose the small tight loop | Active |
| 2026-08-13 | Chart tooltip width set to 17% of the plot rather than a pixel value; cards moved into clear space with leader lines back to their markers | Markers sit 220 and 201 units apart in a 1060-unit viewBox, so any centred card under 19% cannot collide at any viewport size. Cards anchor on the edge nearest a curve so they grow away from it if text rewraps | Active |
| 2026-08-13 | Full-viewport sections use `calc(100svh - var(--nav-h))`, not `100vh` | `SiteShell` already offsets the fixed nav with `padding-top: var(--nav-h)`, so a `100vh` section overshoots by the nav height and clips its own bottom content. `svh` also protects against mobile browser chrome | Active |
| 2026-08-13 | Core thought and "What to expect in 2027" rebuilt as pinned scroll scenes | Both should reveal as the user scrolls through them rather than simply scrolling into view. Core thought builds word by word; the 2027 layers translate at the 0.05/0.12/0.2/0.3 rates from the Figma annotation | Active |
| 2026-08-13 | Chart markers and tooltips reveal individually as the drawing line reaches each one, keyed to arc-length fractions of each path | User feedback: nodes should appear one at a time as the line builds, not all at once at the end | Active |
| 2026-08-14 | Landing page rebuilt from Figma node 114:9 as five full-viewport sections (Hero, Executive Summary, Trend 1, Trend 2, About us); footer left byte-for-byte unchanged | Direct user instruction; footer frame in Figma is annotated "keep this as is" | Active |
| 2026-08-14 | Landing page display type is sans (DM Sans standing in for Graphik), not the serif | Figma redesign sets every headline in Graphik Semibold. User chose "follow Figma exactly", superseding design.md's "never a sans-serif H1 on a hero" for this page | Active |
| 2026-08-14 | About-us headshots carry a 20px border-radius, overriding the global `border-radius: 0 !important` | Specified by Figma nodes 218:635/637, 133:2140, 145:863/865, 218:640. Scoped to `.lp-shot` only, so the no-radius rule still holds everywhere else | Active |
| 2026-08-14 | "About us" heading set in `--purple-dk` (#7500C0) at display size | Figma node 145:785. 7.1:1 on white, so the purple carries as body-grade contrast rather than needing large-text allowance | Active |
| 2026-08-14 | Trend 1 section reuses the Core Thought backdrop from `/trend-1-scroll` — the Alex Brush wordmark drifting as a seamless marquee — laid out as two rows (top and bottom) per the Figma frame | User asked for "the same moving backdrop"; Figma node 284:671 places the wordmark twice. Same font, opacity (white/0.06) and technique as `.t1s-core__mark`, retracked so a -50% translate loops exactly | Active |
| 2026-08-14 | Every other section backdrop animates: drifting radial orbs (Hero, Executive Summary, About us) and a 48s Ken Burns pan on the Trend 2 photograph | User asked for movement on the other backgrounds. Transform/opacity only, and `LiveBackdrop` pauses each set via IntersectionObserver so the five full-height sections never composite at once | Active |
| 2026-08-14 | Trend 2 photo wash deepened from Figma's flat `rgba(0,0,0,0.5)` to a 0.86 → 0.44 horizontal gradient | White body copy over the pale receipts photograph measured ~2.9:1 at 50%, below the 3:1 large-text floor. The graded wash puts the copy over 9:1 and still shows the photograph on the right | Active |
| 2026-08-14 | Hero "Scroll" CTA carries a white hairline | The purple fill sits on the gradient's light zone with ~1.4:1 edge contrast, failing WCAG 1.4.11 for non-text UI. The hairline gives the control a 3:1 boundary without changing the Figma fill | Active |
| 2026-08-14 | Six About-us headshots downloaded from Figma and committed to `public/about/` | Figma MCP asset URLs expire after 7 days, so the bytes had to be stored locally. Sources were served at two resolutions each; the larger was taken and resized to 600px | Active |
| 2026-08-14 | Madeleine's and Sophia's headshots re-cropped tighter onto head and shoulders | User feedback; both sources were wider than the other four, so the row read inconsistently. Cropped from the full-resolution originals rather than the stored 600px versions, so no quality is lost | Active |
| 2026-08-14 | `scroll-margin-top: var(--nav-h)` on every landing section | An in-page anchor parks the target at y=0, which is behind the fixed nav, so the last nav-h of a `100svh - nav-h` section fell past the fold. Verified: the "Scroll" CTA now lands Executive Summary with 0px gap under the nav and 0px overhang past the fold | Active |
| 2026-08-14 | Landing sections joined by `.lp-blend` bands (40vh, 20vh between the two trends, 26/14vh under 760px) | User asked for backgrounds to blend gradually on scroll. Same eased stop recipe as `.t1s-blend` on `/trend-1-scroll`. Bands add scroll distance, but the "Scroll" CTA anchors on the section itself so it still jumps straight to the summary, as requested | Active |
| 2026-08-14 | `LiveBackdrop` is edge-masked so orbs, wordmark, photograph and washes fade to zero at each section boundary | Leaves every section edge as its own flat base colour, so a band only has to dissolve two solid colours and no seam shows. The ramp only has to reach zero at the edge, so it stays short (8–16%) and the backdrops survive intact through the body of each section | Active |
| 2026-08-14 | Hero and Executive Summary base gradients re-angled to 172deg / 176deg (from 163deg / 140deg) | A diagonal base makes a section's bottom edge vary horizontally, which no single blend colour can meet. Near-vertical keeps the edge effectively flat; the orbs still supply the diagonal interest | Active |
| 2026-08-14 | About us internals sized against viewport height (`svh`) above 760px so the section fits one screen; below 760px every Figma value stands | User asked for the section to fit in the viewport "within reason". Headshots run `clamp(120px, 17svh, 200px)`, so they reach the full 200px on tall screens and floor at 120px rather than shrinking indefinitely. Captions are `white-space: nowrap` and allowed to run into the gutter, so a shrinking tile can never grow the row back by rewrapping. Measured: fits with 75px spare at 1512×1080, 26px at 1440×900, and overruns by 2px at 1440×700 | Active |
| 2026-08-14 | About us into the footer left as a hard cut, unblended | The footer is site chrome rather than a landing section, is annotated "keep this as is" in Figma, and cuts the same way on every other page | Active |
| 2026-08-18 | Scrollytelling build promoted to `/trend-1`; the old flip-card page is replaced and `/trend-1-scroll` redirects | User decision. Both trend pages must behave identically, so the scroll build becomes the real Trend 1 and Trend 2 gets rebuilt the same way. Supersedes the 2026-08-13 parallel-route row | Active |
| 2026-08-18 | Page shell extracted to `components/scroll/TrendScrollPage` driven by `src/content/trends/*`; CSS prefix renamed `t1s-` to `trs-` | Trend 2 is next and must match exactly. Everything that differs between the two trends is now data, so the two pages share one component tree and one stylesheet by construction | Active |
| 2026-08-18 | Trend 1 core thought replaced with the Figma 142:8559 wording, propagated to the landing page and `page-content/` | New copy from the redesign: "People are reassessing how they express individuality… What stands out now is not access, but authorship." | Active |
| 2026-08-18 | Signal heading moved into the left column above the image collage; prose split into per-screenful blocks in the right rail; nothing sticks | Figma nodes 110:3040 / 144:8585 put the heading top-left with the drops falling beneath it. User instruction: signal titles scroll with the copy rather than pinning | Active |
| 2026-08-18 | Section rail labels use the real signal names ("Early proof of authorship") rather than "Signal 1" | User instruction. The rail doubles as a summary of the argument | Active |
| 2026-08-18 | Each word drop set in its own display face, loaded only on the trend pages | Figma sets every word in a different typeface deliberately. Three substitutions were needed: Shippori Mincho for Kokoro, Comic Neue for Comic Sans MS, Poppins for Geom — none of the originals are served by Google Fonts | Active |
| 2026-08-18 | Word drops centred on Figma's centre point, not their left edge, via the `translate` property | Figma centres eight of the twelve words with `-translate-x-1/2`. Anchoring by left edge made every substituted typeface push its word off the right edge. `translate` is used so the shared parallax loop keeps sole ownership of `transform` | Active |
| 2026-08-18 | "What to expect in 2027" is now type only; the three parallax photo layers are gone | The redesigned frame (110:3067) contains no images. User chose to follow the frame over the older parallax annotation still attached to it. The pinned sequential reveal stays | Active |
| 2026-08-18 | Implication flip cards replaced by the Figma card carousel (node 356:683), built on native scroll-snap | Swipe, trackpad, scrollbar and Tab-to-card all work without custom code, and the browser owns the scroll physics. Arrows and the stepper call `scrollTo` | Active |
| 2026-08-18 | Implication cards carry a 12px border-radius, overriding the global `border-radius: 0 !important` | Specified by Figma nodes 446:797-801 and the card component. Scoped to `.trs-card__inner` only, as `.lp-shot` is on the landing page | Active |
| 2026-08-18 | Card photo wash graded (0.72 at the top down to 0.40) instead of Figma's flat `rgba(0,0,0,0.4)` | White copy over the brightest cards measured 2.85:1 against cloud highlights at a flat 0.4. Measured after the change: title worst case 5.28:1, body 5.39:1 in the revealed state | Active |
| 2026-08-18 | Card and signal imagery downloaded from Figma into `public/trend-1/` and `public/trend-2/` as WebP | Figma MCP asset URLs expire after 7 days. Sources were up to 16MB JPEG/PNG; WebP at 800px brings the whole set to ~1.6MB with no visible loss. Trend 2's six card fills were grabbed in the same pass so the build is not blocked on expired URLs | Active |
| 2026-08-18 | `backdrop-filter` written unprefixed only | Writing `-webkit-backdrop-filter` alongside it made Lightning CSS collapse the pair to the prefixed property alone, which Chromium ignores, silently removing the blur from the card reveal. The build adds prefixes for its own targets | Active |
